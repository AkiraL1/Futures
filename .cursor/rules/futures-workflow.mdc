# Futures Workflow — 项目级协作规则

> 本文件是 **Cursor 项目级规则**，对所有会话生效。
> 与 [`MODULE_GUIDE.md`](../../MODULE_GUIDE.md) 中的 §5「信息验证规范」保持完全一致；任何修改必须两边同步。
> 本规则由 `law/.cursor/rules/legal-workflow.mdc` 迁移而来，按期货品种分析场景做了术语与示例重写。

---

## 1. 模块边界（强制）

- 每次打开任何文件前，**先阅读 [`MODULE_GUIDE.md`](../../MODULE_GUIDE.md)** 确定目标模块
- 修改前必须声明：
  - **Target Module**：`<模块名>`
  - **Target Files**：`<文件 1, 文件 2>`
  - **Intent**：`<变更内容>`
- **禁止**在未声明的情况下跨模块修改
- **禁止**为节省时间跳过 `MODULE_GUIDE.md`

## 2. 文件大小与拆分

- 新文件 ≤ 300 行；推荐 ≤ 200 行
- 已存在的大文件 **不主动重构**，除非用户明确要求
- 合约分析文档超长时按章节拆分子文件（`cases/<合约>/01-事实.md` 等）

## 3. 隐私保护（强制）

- **绝不**在对话中输出或写入 `profile/trading-profile.md` 的完整内容
- **绝不**将完整账户号、完整持仓、券商凭证等写入任何提交文件
- 写入新文档时如需引用事实，使用**相对路径引用**：

```markdown
参见 [profile/trading-profile.md §3 资金与仓位](../../profile/trading-profile.md#3-资金与仓位)
```

- 修改或新建文件前，**先检查 `.gitignore`** 是否生效（`git check-ignore -v <文件>`）

## 4. 信息验证规范（核心强制规则）⚠️

### 4.1 原则

- **期货市场信息必须严谨交叉验证** — 合约规则、交易所公告、监管政策、历史价格、持仓数据、交割规则、保证金比例，**不得仅凭模型记忆直接写入**
- **默认信息源优先级**：
  1. **官方一手源**：交易所官网公告（CME Group / CBOT / NYMEX / COMEX / ICE / DCE / SHFE / CZCE / INE / GFEX 等）
  2. **监管机构**：CFTC、SEC、CSRC（中国证监会）、国务院期货监督管理办公室
  3. **清算所与结算机构**：CME Clearing、SHCH 等
  4. **权威数据提供商**：Bloomberg、Reuters、Wind、Choice、同花顺
  5. **交易所公开行情与公告栏公告**
  6. **券商研报、行业媒体**（仅作为线索，需核验）

### 4.2 强制使用 ego-browser ⚠️

**触发场景**（任一即必须使用）：

- 引用某合约**具体规格**（代码、合约乘数、最小变动价位、保证金、交割方式、交割品级）
- 引用某**交易所规则编号、年份、章节**（如 CBOT Rulebook 章节）
- 引用某**监管公告**（CFTC 持仓报告 COT、SHFE 风险控制公告、CSRC 监管通知）
- 引用某**历史价格数据、持仓量、成交量**（必须有数据源 URL + 核验日期）
- 引用某**保证金比例、涨跌停板、限仓规则**
- 引用某**宏观经济数据**（USDA 报告、OPEC 决议、EIA 库存、BLS 非农、CPI 等）
- 用户提出"帮我查一下…"、"现在还生效吗"等明确检索需求

**使用流程**：

1. **观察**：`snapshotText()` 获取可访问性树
2. **执行**：`click` / `fillInput` / `cdp`（必要时）
3. **再观察**：每次操作后 `snapshotText()` 或 `captureScreenshot()` 确认
4. **记录**：将验证过的关键事实连同官方源 URL 写入文档

**调用方式**：

```bash
ego-browser nodejs <<'EOF'
const task = await useOrCreateTaskSpace('verify CBOT ZC corn futures specs')
await openOrReuseTab('https://www.cmegroup.com/markets/agriculture/grains-and-oilseeds/corn.contractSpecs.html', { wait: true })
cliLog(await snapshotText())
EOF
```

**不使用 ego-browser 的例外**：纯本地文档整理、引用既有文档内容。

### 4.3 文档中的强制标注

引用合约规格 / 规则 / 数据时**必须同时**给出：

1. **精确引用**：如 `CBOT Corn Futures (ZC), Contract Size: 5,000 bushels, Tick Size: 1/4 cent per bushel ($12.50)`
2. **官方源 URL**：CME Group / Exchange Notice / CFTC / USDA 等
3. **最后核验日期**：如 `🗓 最后核验：2026-09-13`
4. **核验方式**：如 `✅ ego-browser 核验` / `🟡 仅引用既有文档` / `🟠 无法自动核验 — 待人工确认`

**反例**（禁止）：

> ❌ "玉米期货合约乘数是 5000 蒲式耳，最小变动 0.25 美分。"

**正例**（必须照此写）：

> ✅ "CBOT Corn Futures (ZC), Contract Size: **5,000 bushels**, Tick Size: **1/4 cent per bushel ($12.50)**（来源：<https://www.cmegroup.com/markets/agriculture/grains-and-oilseeds/corn.contractSpecs.html>）。🗓 最后核验：2026-09-13 via ego-browser + CME Group Website。"

### 4.4 时效性管理

- 合约规格可能修订 → 引用时附核验日期
- 保证金比例、涨跌停板随行情调整 → 引用时写明**生效日期**
- USDA、EIA、OPEC 等月报 / 年报数据 → 必须标注**报告期**
- 用户在关键事项（建仓 / 平仓 / 交割 / 合规）做决定前 → **必须重新核验**

### 4.5 不可验证情形

若 ego-browser 无法访问目标源：

1. **不**继续凭记忆编造
2. **明确告知用户**："该项信息源当前无法验证，建议人工核对 [官方源链接]"
3. 在文档中标注 `🟠 无法自动核验 — 待人工确认`

### 4.6 ego-browser 任务空间管理（强制流程）

> 与 `MODULE_GUIDE.md` §5.8 完全一致；本节是其 AI 侧的可执行版本。

**核心原则**：

- **每个 ego-browser task space 都必须被显式关闭**，**不得**让任务空间随会话遗留
- 一次"用户目标"对应一个 task space；同一目标的所有 heredoc 轮次复用同一 task space
- 当目标已完成（无论成功、失败或放弃），**立即**在最后一个 heredoc 中调用 `completeTaskSpace(name, { keep })`
- 默认 `{ keep: false }`；仅在以下情况才使用 `{ keep: true }`：
  - 用户明确要求保留页面
  - 需要用户在该页面手动操作（如登录 Bloomberg Terminal）
  - 结果无法以 URL / 文件 / 摘要形式交付

**使用流程（强制 7 步）**：

1. `useOrCreateTaskSpace('<短名>')` — 命名反映任务意图
2. `cliLog('task space id: ' + task.id)` — 把 `task.id` 用于后续轮次复用
3. `openOrReuseTab(url, { wait: true })` — 同一目标后续页面切换复用同一 tab
4. `snapshotText()` / `js()` / `cdp()` 等 — 观察 → 操作 → 再次观察
5. **每个 heredoc 开头**调用 `useOrCreateTaskSpace(nameOrId)` 跨轮次保留同一 task space
6. **目标完成时**调用 `completeTaskSpace(nameOrId, { keep: false })`
7. 关闭时若结果 `{ done: false, skipped: ... }`，立即重试或向用户报告，**不得**假装完成

**跨轮次复用的硬性规则**：

- 同一用户目标的**所有后续 heredoc** 必须在开头调用 `useOrCreateTaskSpace('<目标短名>')`，**不得**为同一目标创建新 task space
- 仅在以下情况才创建**新** task space：
  - 用户开始**新目标**（与旧目标无关）
  - 用户明确要求开新空间
  - 原 task space 不可用且用户已确认
- 创建新 task space 时必须**说明原因**

**应关闭 vs 保留的场景**：

- ✅ **应关闭**：核验全部完成并写入文档 / 检索失败并标注"无法自动核验" / 用户明示放弃任务
- 🟢 **可保留 `{ keep: true }`**：用户明确说"保留这个页面" / 用户需要在该页面手动操作
- ❌ **不应保留**：仅因"访问过" / "创建了文档" / "用了一次截图" 就保留页面

## 5. 引用规范（强制）

### 5.1 引用格式

- **合约规格**：`《合约名称》, <交易所代码>, <字段>:<值>`，如 `CBOT Corn Futures (ZC), Contract Size: 5,000 bushels`
- **交易所规则**：`《Rulebook Name》, <Exchange>, <Chapter/Section>`，如 `CBOT Rulebook, Chapter 7 - Delivery`
- **监管公告**：`《<机构> <公告类型>》, <编号>, <日期>`，如 `CFTC Commitment of Traders Report, 2026-09-09`
- **官方页面**：完整 URL + 页面标题 + 最后访问日期
- **跨模块引用**：相对路径，如 `[profile/trading-profile.md](../../profile/trading-profile.md#3-资金与仓位)`

### 5.2 引用必须附带元信息

每条合约规格 / 规则 / 数据 / 监管信息引用必须**同时**给出：

1. **精确引用**（如 `ZC contract size 5,000 bushels`）
2. **官方源 URL**（CME Group / Exchange / USDA / CFTC / SHFE 等）
3. **最后核验日期**（如 `🗓 最后核验：2026-09-13`）
4. **核验方式**（`✅ ego-browser 核验` / `🟡 仅引用既有文档` / `🟠 无法自动核验 — 待人工确认`）

### 5.3 中英双语呈现（与 §6 配合执行）

引用合约规格 / 规则 / 官方术语 / 程序步骤时必须**同时给出英文原文与中文严谨翻译**，详见 §6「中英双语要求」。

## 6. 中英双语要求（强制）

> 📌 本节是对 §4.3「文档中的强制标注」和 §5「引用规范」的语言形式补充。任何 `modules/<品种>/cases/`、`modules/<品种>/notes/`、`profile/` 中的分析文档涉及合约规格 / 规则 / 官方术语 / 程序步骤时，**必须**按本节双语呈现。

### 6.1 双语格式

**A. 合约规格 / 字段**

```
英文原文：<Contract Size: 5,000 bushels; Tick Size: 1/4 cent per bushel ($12.50)>
中文严谨翻译：合约单位：5,000 蒲式耳；最小变动价位：每蒲式耳 1/4 美分（$12.50）
```

**反例**（禁止）：

> ❌ "玉米期货合约乘数是 5000 蒲式耳，最小变动 0.25 美分。"

**正例**（必须照此写）：

> ✅ "Contract Size: **5,000 bushels**; Tick Size: **1/4 cent per bushel ($12.50)**"
> 中文严谨翻译：合约单位：**5,000 蒲式耳 / bushels**；最小变动价位：每蒲式耳 **1/4 美分 / cent（$12.50）**。

**B. 监管公告 / 政策**

```
英文原文：<CFTC Commitment of Traders Report (COT), as of September 9, 2026>
中文严谨翻译：CFTC 交易者持仓报告（Commitment of Traders Report, COT），截至 2026 年 9 月 9 日
```

**C. 交易所术语**

```
英文原称：<Initial Margin>
中文严谨翻译：初始保证金（Initial Margin）
首次出现后可在同文档内缩写为：Initial Margin / 初始保证金
```

**D. 程序 / 时限 / 字段名**

```
英文原文：submit delivery application through CME ClearPort
中文严谨翻译：通过 CME ClearPort（CME 清算门户系统）提交交割申请
```

### 6.2 重点术语保留英文

下列术语**首次出现时**必须采用 `中文译名 / English Term` 格式；后续可仅用中文译名：

- contract size（合约单位 / 合约乘数）、tick size（最小变动价位）、daily price limit（涨跌停板）、position limit（持仓限额）
- initial margin（初始保证金）、maintenance margin（维持保证金）、variation margin（变动保证金）
- physical delivery（实物交割）、cash settlement（现金交割）、first notice day（第一通知日，FND）、last trade day（最后交易日，LTD）、last notice day（最后通知日，LND）
- open interest（持仓量）、volume（成交量）、settlement price（结算价）
- contango（期货升水）、backwardation（期货贴水）、basis（基差）
- long position（多头 / 多仓）、short position（空头 / 空仓）、hedging（套保）、speculation（投机）、arbitrage（套利）
- Commitment of Traders（交易者持仓报告，COT）、disaggregated COT（分类持仓报告）
- fair value（理论价）、fair value gap（理论价差）

完整清单随 `notes/` 积累动态扩展。

### 6.3 翻译质量要求

- **严谨优先**：期货语境下中文翻译必须符合对应交易所惯例；歧义处保留英文原文
- **不增不减**：不得省略原文中 "must"、"shall"、"may" 等情态动词的关键含义
- **不创造**：不得凭模型记忆补全未在原文中出现的内容
- **核验过的原文优先**：凡 ego-browser 已核验过的合约规格 / 规则原文，应**优先采用核验版**而非凭记忆复述

### 6.4 已知例外

- **纯本地整理 / 流程性清单**（如 `priority-checklist.md`）：可不强制双语，但首次出现英文专有名词仍需括注
- **代码 / 配置文件 / 命令**：不属于本要求范围
- **表格中已脱敏的数据字段**（如 "Account: XXX-XXXX"）：不强制双语

## 7. 文档模板

`modules/<品种>/cases/<合约>/01-事实.md` 等推荐结构（与 `MODULE_GUIDE.md` §4.1 一致）：

1. **背景事实**（引用 `profile/`，避免重复）
2. **分析焦点 / 待解决的品种问题**
3. **合约规格与适用规则**（每条须含 URL + 核验日期 + 核验方式 + 中英双语）
4. **基本面 / 技术面 / 政策面分析过程**
5. **结论与建议**
6. **风险与待办**

## 8. 修改记录同步

修改本规则文件时，必须同时更新 [`MODULE_GUIDE.md`](../../MODULE_GUIDE.md) §5「信息验证规范」与 §5.7「中英双语要求」；反之亦然。三处为**单一事实来源的多份镜像**，任一处修改必须同步全部。

---

> 📌 **核验入口**：当你需要在文档中写入任何合约规格 / 规则 / 数据 / 报告时，请主动调用 `ego-browser` 进行交叉验证。这不是可选优化，而是**项目级硬性要求**。
> 📌 **双语入口**：任何引用合约规格 / 规则 / 官方术语 / 程序步骤时，必须**英文原文 + 中文严谨翻译**双行呈现，重点术语首次出现时附「中文译名 / English Term」括注。这不是风格建议，而是**项目级硬性要求**。